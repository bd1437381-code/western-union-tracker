{
  "buildCommand": "npm install -g pnpm && pnpm install && pnpm --filter @workspace/western-union-clone run build",
  "outputDirectory": "artifacts/western-union-clone/dist/public",
  "installCommand": "npm install -g pnpm && pnpm install",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
